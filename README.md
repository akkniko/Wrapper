# wrapper lab


An example of how to run the project:

```bash
mkdir build
cd build
cmake .. 
cmake --build .
```

To start the wrapper program, type the command:

```bash
cd build
./MethodEncapsulation.exe
```


